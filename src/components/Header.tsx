export default function Header() {
  return (
    <header className="text-center flex-shrink-0">
      <h1 className="text-3xl sm:text-4xl md:text-5xl font-bold tracking-wider">
        <span className="text-[#5DADE2]" style={{ textShadow: '2px 2px 4px rgba(0,0,0,0.2)' }}>
          NOVO
        </span>
        <span className="text-[#4A4A4A]" style={{ textShadow: '2px 2px 4px rgba(0,0,0,0.2)' }}>
          PRESENT
        </span>
      </h1>
    </header>
  )
}
